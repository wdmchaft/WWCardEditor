


Flow field: selection behavior:
- Get tabbing and reverse-tabbing to work
- Need a way to select none (where active field changes to NSNotFound)

Flow field: drawing:
- Placeholder support
- Shadow should only draw if containing window has focus
- Should use floor/ceil + 0.5 to avoid drawing on pixel cracks

Flow field: editing:
- Start on editing
- Handle dragging and dropping across fields
